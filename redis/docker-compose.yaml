services:
  redis-db:
    image: redis:8.2.7
    container_name: redis-db
    env_file:
      - .env
    restart: always
    command: redis-server --requirepass ${REDIS_PASSWORD}
    ports:
      - "6379:6379"